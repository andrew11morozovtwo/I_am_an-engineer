# Сбор данных и отправка в ИИ при комментариях пользователей

## Обзор

Этот документ описывает, какие данные бот собирает из комментариев пользователей и что отправляется в OpenAI API для генерации ответов.

---

## 🔍 Какие данные собираются из комментария пользователя

Бот обрабатывает комментарий пользователя через функцию `prepare_message_content()` и собирает следующие данные:

### 1. **Текстовый контент**
- **Текст сообщения** (`message.text`) — если комментарий содержит только текст
- **Подпись к медиа** (`message.caption`) — если комментарий содержит медиа с подписью
- **Обработка URL** — если в тексте есть ссылки, бот извлекает текст с веб-страниц (с таймаутом 10 секунд)

### 2. **Изображения (фото)**
- Если комментарий содержит фото, бот:
  - Получает URL изображения через Telegram API
  - Отправляет изображение в **OpenAI Vision API** для получения описания
  - Добавляет описание изображения к контенту комментария

### 3. **PDF документы**
- Если комментарий содержит PDF файл, бот:
  - Скачивает файл через Telegram API
  - Извлекает весь текст из PDF (без анализа через OpenAI)
  - Добавляет извлеченный текст к контенту комментария

### 4. **Другие документы**
Поддерживаемые форматы:
- `.txt` — текстовые файлы
- `.docx` — документы Word
- `.xlsx` — таблицы Excel
- `.pptx` — презентации PowerPoint
- `.odt` — документы OpenDocument

Для каждого типа документа бот:
- Скачивает файл через Telegram API
- Извлекает текст из документа (без анализа через OpenAI)
- Добавляет извлеченный текст к контенту комментария

### 5. **Голосовые сообщения**
- Если комментарий содержит голосовое сообщение (`message.voice`), бот:
  - Скачивает аудио файл через Telegram API
  - Отправляет аудио в **OpenAI Whisper API** для транскрибации
  - Добавляет транскрибированный текст к контенту комментария

### 6. **Аудио файлы**
- Если комментарий содержит аудио файл (`message.audio`), бот:
  - Скачивает аудио файл через Telegram API
  - Отправляет аудио в **OpenAI Whisper API** для транскрибации
  - Добавляет транскрибированный текст к контенту комментария

### 7. **Опросы**
- Если комментарий содержит опрос (`message.poll`), бот:
  - Извлекает вопрос опроса
  - Добавляет вопрос к контенту комментария

---

## 📤 Что отправляется в OpenAI API

### Структура запроса к OpenAI

Когда пользователь оставляет комментарий к посту, бот отправляет в OpenAI API следующий запрос:

```python
messages = [
    {
        "role": "system",
        "content": REPLY_SYSTEM_PROMPT  # Системный промпт с инструкциями для бота
    },
    {
        "role": "user",
        "content": user_message  # Контент комментария пользователя
    }
]
```

### 1. **Системный промпт (REPLY_SYSTEM_PROMPT)**
Это статический текст с инструкциями для ИИ о том, как отвечать на комментарии. Промпт содержит:
- Информацию о канале ("Безопасность всегда")
- Принципы ответа на комментарии
- Стиль общения (вежливость, дружелюбность)
- Примеры хороших и плохих ответов
- Ограничения (максимум 500 знаков)

**Важно:** Системный промпт не содержит никаких личных данных пользователей.

### 2. **Контент комментария пользователя (user_message)**

Формируется следующим образом:

#### Если комментарий является ответом на пост:
```
Пост: {original_post_content}

Комментарий пользователя: {user_comment}
```

Где:
- `original_post_content` — полный контент оригинального поста (обработанный через `prepare_message_content()`)
- `user_comment` — полный контент комментария пользователя (обработанный через `prepare_message_content()`)

#### Если комментарий не является ответом на пост:
```
{user_comment}
```

Где `user_comment` — полный контент комментария пользователя.

### 3. **Параметры запроса к OpenAI**
```python
{
    "model": "gpt-4o-mini",
    "messages": messages,  # Описано выше
    "max_tokens": 300,     # Максимум 300 токенов для ответа
    "temperature": 0.7     # Уровень креативности (0.7 = умеренная креативность)
}
```

---

## ❌ Что НЕ отправляется в OpenAI API

Бот **НЕ отправляет** следующие данные в OpenAI API:

### Личные данные пользователя:
- ❌ **User ID** (`message.from_user.id`) — не отправляется
- ❌ **Username** (`message.from_user.username`) — не отправляется
- ❌ **Имя пользователя** (`message.from_user.first_name`, `message.from_user.last_name`, `message.from_user.full_name`) — не отправляется
- ❌ **Язык пользователя** (`message.from_user.language_code`) — не отправляется

### Метаданные сообщения:
- ❌ **Message ID** (`message.message_id`) — не отправляется
- ❌ **Chat ID** (`message.chat.id`) — используется только локально для истории разговора, но не отправляется в API
- ❌ **Дата и время сообщения** (`message.date`) — не отправляется
- ❌ **Тип чата** (`message.chat.type`) — не отправляется

### Другие данные:
- ❌ **История предыдущих сообщений** — не отправляется (используется только локально в памяти бота с ограничениями)
- ❌ **Статус пользователя** (забанен/не забанен) — не отправляется
- ❌ **Количество варнов пользователя** — не отправляется
- ❌ **IP-адрес или другая техническая информация** — не отправляется

---

## 🔄 История разговоров (локальное хранение)

Бот хранит историю разговоров **локально в памяти** (в словаре `conversation_history`), но с ограничениями:

- Максимум **10 чатов** с историей
- Максимум **3 сообщения** в истории на чат (user + assistant пары)
- История используется только для контекста при генерации ответов
- История **НЕ отправляется** в OpenAI API при каждом запросе (используется только для формирования контекста локально)

**Важно:** История разговоров хранится только в оперативной памяти бота и не сохраняется в базе данных. При перезапуске бота история теряется.

---

## 📊 Примеры данных, отправляемых в OpenAI

### Пример 1: Простой текстовый комментарий

**Комментарий пользователя:**
```
"Спасибо за полезную информацию! Как часто нужно проверять огнетушители?"
```

**Что отправляется в OpenAI:**
```json
{
  "messages": [
    {
      "role": "system",
      "content": "Вы бот-администратор в телеграм-канале 'Безопасность всегда'..."
    },
    {
      "role": "user",
      "content": "Пост: [контент оригинального поста]\n\nКомментарий пользователя: Спасибо за полезную информацию! Как часто нужно проверять огнетушители?"
    }
  ],
  "model": "gpt-4o-mini",
  "max_tokens": 300,
  "temperature": 0.7
}
```

### Пример 2: Комментарий с фото

**Комментарий пользователя:**
- Фото с подписью: "Вот как я организовал систему безопасности на работе"

**Что отправляется в OpenAI:**
```json
{
  "messages": [
    {
      "role": "system",
      "content": "Вы бот-администратор..."
    },
    {
      "role": "user",
      "content": "Пост: [контент поста]\n\nКомментарий пользователя: Вот как я организовал систему безопасности на работе\n\nОписание изображения: [описание фото, полученное через Vision API]"
    }
  ],
  "model": "gpt-4o-mini",
  "max_tokens": 300,
  "temperature": 0.7
}
```

### Пример 3: Комментарий с голосовым сообщением

**Комментарий пользователя:**
- Голосовое сообщение (транскрибируется в текст)

**Что отправляется в OpenAI:**
```json
{
  "messages": [
    {
      "role": "system",
      "content": "Вы бот-администратор..."
    },
    {
      "role": "user",
      "content": "Пост: [контент поста]\n\nКомментарий пользователя: [транскрибированный текст из голосового сообщения]"
    }
  ],
  "model": "gpt-4o-mini",
  "max_tokens": 300,
  "temperature": 0.7
}
```

---

## 🔒 Безопасность и конфиденциальность

### Что защищено:
1. **Личные данные пользователей** (ID, username, имя) не отправляются в OpenAI API
2. **Метаданные сообщений** не отправляются в OpenAI API
3. **История разговоров** хранится только локально в памяти и не сохраняется в базе данных

### Что отправляется:
1. **Только контент сообщения** (текст, описание изображений, транскрипции аудио, текст из документов)
2. **Контент оригинального поста** (если комментарий является ответом на пост)
3. **Системный промпт** (статический текст с инструкциями)

### Рекомендации:
- Пользователи должны понимать, что **контент их комментариев** (текст, изображения, аудио, документы) отправляется в OpenAI API для генерации ответов
- Если пользователь не хочет, чтобы его контент обрабатывался ИИ, он может не комментировать посты
- Администраторы могут отключить функцию генерации ответов через ИИ, если это необходимо

---

## 📝 Технические детали

### Файлы, отвечающие за обработку данных:

1. **`app/application/services/content_service.py`**
   - Функция `prepare_message_content()` — собирает и обрабатывает контент из сообщения
   - Функции для обработки различных типов медиа (фото, PDF, документы, аудио)

2. **`app/application/services/comment_service.py`**
   - Функция `generate_reply_to_comment()` — формирует запрос к OpenAI API
   - Системные промпты (`REPLY_SYSTEM_PROMPT`)

3. **`app/presentation/routers/channel_router.py`**
   - Обработчик `discussion_message_handler()` — обрабатывает комментарии пользователей
   - Вызывает `prepare_message_content()` и `generate_reply_to_comment()`

### Ограничения и таймауты:

- **Обработка URL в тексте:** таймаут 10 секунд
- **Описание изображений:** таймаут 30 секунд
- **Извлечение текста из PDF:** таймаут 60 секунд
- **Извлечение текста из документов:** таймаут 60 секунд
- **Транскрибация аудио:** таймаут 60 секунд
- **Максимальный размер файла:** 10 MB
- **Максимальная длина текста:** 8000 символов

---

## 🔍 Как проверить, что отправляется в OpenAI

Для отладки можно включить логирование в следующих местах:

1. **В `channel_router.py` (строка 603):**
   ```python
   logger.info(f"Полный контент комментария пользователя для генерации ответа: {user_comment_full[:200]}...")
   ```

2. **В `comment_service.py` (можно добавить):**
   ```python
   logger.debug(f"Отправка в OpenAI: {messages}")
   ```

3. **В `content_service.py` (строка 681):**
   ```python
   logger.info(f"Подготовка контента завершена. Результат ({len(result)} символов): {result[:200]}...")
   ```

---

## 📚 Дополнительная информация

- Системный промпт для ответов на комментарии: `app/application/services/comment_service.py`, строки 58-118
- Функция подготовки контента: `app/application/services/content_service.py`, строки 512-683
- Функция генерации ответа: `app/application/services/comment_service.py`, строки 182-230
