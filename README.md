# AdminBot - Telegram-бот администратор канала

Telegram-бот для автоматизации модерации, управления контентом и взаимодействия с участниками в Telegram-канале.

## 🚀 Возможности

- ✅ Автоматическое отслеживание новых постов в канале
- ✅ Автоматический первый комментарий к новым постам
- ✅ Фильтрация контента по черному списку (blacklist)
- ✅ Обработка комментариев подписчиков
- ✅ База данных для хранения пользователей, банов, варнов, логов
- ✅ Многослойная архитектура (Clean Architecture)

## 📋 Требования

- Python 3.10+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- Telegram канал с включенной группой обсуждений

## 🛠 Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd "I am an engineer"
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Создайте файл `.env` в корне проекта:**
   ```env
   BOT_TOKEN=ваш_токен_от_BotFather
   ADMIN_IDS=ваш_user_id_число
   DB_URL=sqlite+aiosqlite:///app.db
   ```
   
   > **Как получить user_id:** напишите боту [@userinfobot](https://t.me/userinfobot) в Telegram

4. **Инициализируйте базу данных:**
   ```bash
   python -m app.scripts.init_db
   ```

## 🎯 Запуск

```bash
python -m app.main
```

Бот начнет работать и обрабатывать события канала.

## 📁 Структура проекта

```
app/
├── main.py                          # Точка входа
├── config/
│   └── settings.py                  # Конфигурация (pydantic)
├── presentation/                    # Слой представления (Telegram API)
│   ├── routers/
│   │   ├── user_router.py          # Команды пользователей (/start, /help)
│   │   ├── admin_router.py         # Команды администраторов
│   │   ├── moderation_router.py     # Модерация сообщений
│   │   └── channel_router.py       # Обработка постов и комментариев канала
│   └── middlewares/                 # Middleware (логирование, проверка админов)
├── application/                     # Слой приложения (бизнес-логика)
│   └── services/
│       ├── moderation_service.py   # Сервис модерации
│       ├── user_service.py         # Сервис пользователей
│       ├── stats_service.py        # Сервис статистики
│       ├── comment_service.py      # Сервис комментариев
│       ├── schedule_service.py     # Сервис отложенных постов
│       └── ai_service.py           # Сервис ИИ (будущее)
├── domain/                          # Доменный слой
│   ├── entities.py                  # Доменные сущности
│   └── value_objects.py            # Value objects
├── infrastructure/                  # Слой инфраструктуры
│   ├── db/
│   │   ├── models.py               # SQLAlchemy ORM модели
│   │   ├── session.py              # Настройка БД
│   │   └── repositories.py        # Репозитории (CRUD)
│   └── ai/
│       └── llm_client.py           # Клиент для LLM API
├── common/                          # Общие утилиты
│   ├── logging_utils.py
│   ├── exceptions.py
│   └── types.py
└── scripts/
    └── init_db.py                  # Скрипт инициализации БД
```

## ⚙️ Настройка бота в Telegram

1. **Создайте бота через [@BotFather](https://t.me/BotFather):**
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Сохраните полученный токен в `.env`

2. **Добавьте бота в канал:**
   - Откройте настройки канала → Администраторы
   - Добавьте бота как администратора
   - Дайте права на удаление сообщений

3. **Настройте группу обсуждений:**
   - В настройках канала → Обсуждения
   - Создайте или выберите группу для обсуждений
   - Добавьте бота в эту группу как администратора
   - Дайте права на отправку сообщений

## 🔧 Конфигурация

Все настройки находятся в файле `.env`:

- `BOT_TOKEN` - токен бота от BotFather
- `ADMIN_IDS` - список user_id администраторов через запятую (например: `123456789,987654321`)
- `DB_URL` - строка подключения к БД (по умолчанию SQLite)

## 📝 Текущий функционал

### Этап 1 (реализовано):
- ✅ Базовая структура проекта
- ✅ Подключение к Telegram API (aiogram)
- ✅ База данных SQLite с SQLAlchemy
- ✅ Обработка новых постов в канале
- ✅ Автоматический первый комментарий к постам
- ✅ Обработка комментариев подписчиков
- ✅ Фильтрация по черному списку (blacklist)
- ✅ Регистрация пользователей при команде /start

### Планируется:
- [ ] Админ-команды (/ban, /warn, /blacklist, /stats)
- [ ] Антифлуд система
- [ ] Логирование действий
- [ ] Интеграция с ИИ (LLM)
- [ ] Отложенная публикация постов
- [ ] Статистика и отчеты

## 🐛 Отладка

Если бот не отвечает:

1. Проверьте, что бот запущен (`python -m app.main`)
2. Убедитесь, что бот добавлен как администратор канала и группы обсуждений
3. Проверьте права бота (удаление сообщений, отправка сообщений)
4. Проверьте файл `.env` с правильным токеном
5. Проверьте логи в консоли

## 📄 Лицензия

Проект разработан в рамках технического задания.

## 👤 Автор

Разработано для автоматизации модерации Telegram-канала.

---

**Версия:** 1.0.0 (Этап 1 - Базовый функционал)
