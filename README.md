# AdminBot - Telegram Bot Administrator

Telegram-бот администратор для модерации канала с поддержкой динамического управления администраторами, автоматической модерации, системы предупреждений и банов.

## 📋 Текущее состояние разработки

### ✅ Реализованная функциональность

#### 1. Автоматическая модерация
- ✅ Проверка всех сообщений на наличие запрещенных фраз из черного списка
- ✅ Использование регулярных выражений для обнаружения матерных слов (русский и английский языки)
- ✅ Проверка контента из различных источников:
  - Текст сообщений и подписи к медиа
  - Описания изображений (через OpenAI Vision API)
  - Текст из PDF документов
  - Текст из документов (txt, docx, xlsx, pptx, odt)
  - Транскрипция голосовых сообщений и аудио файлов (через Whisper API)
- ✅ Автоматическое удаление сообщений с запрещенным контентом
- ✅ Выдача предупреждений (варнов) при нарушении правил
- ✅ Автоматический бан на 24 часа при накоплении 3+ предупреждений
- ✅ Автоматическое снятие истекших банов (проверка каждый час)
- ✅ Уведомления пользователям о предупреждениях и банах в личные сообщения

#### 2. Система управления администраторами (динамическая)
- ✅ Хранение администраторов в базе данных (вместо статического `.env`)
- ✅ Три роли администраторов: `owner`, `senior_admin`, `moderator`
- ✅ Иерархия прав доступа:
  - **Owner** — может добавлять/удалять всех администраторов, менять любые роли
  - **Senior Admin** — может добавлять/удалять модераторов, менять роли модераторов
  - **Moderator** — базовые права модерации (ban/warn/blacklist)
- ✅ Команды управления администраторами:
  - `/addadmin {user_id} {role}` — добавить администратора
  - `/removeadmin {user_id}` — удалить администратора (soft delete)
  - `/admins` — список всех администраторов с user_id
  - `/myadmin` — информация о себе как администраторе
  - `/setrole {user_id} {new_role}` — изменить роль администратора
- ✅ Инициализация администраторов из `.env` при первом запуске (первый админ получает роль `owner`)

#### 3. Команды модерации
- ✅ `/ban {user_id} {reason}` — заблокировать пользователя (временно или навсегда)
- ✅ `/warn {user_id} {reason}` — выдать предупреждение (3 варна = автоматический бан)
- ✅ `/blacklist add {phrase}` — добавить фразу в черный список
- ✅ `/blacklist remove {phrase}` — удалить фразу из черного списка
- ✅ `/blacklist list [page]` — показать черный список (постранично, 10 фраз на страницу)
- ✅ `/stats` — статистика бота (пользователи, баны, варны, логи)

#### 4. Пользовательские команды
- ✅ `/start` — регистрация в системе (показывает Telegram ID)
- ✅ `/help` — справка по командам (для всех пользователей, админы видят дополнительные команды)
- ✅ `/myid` — показать свой Telegram user_id (для получения ID для команд админов)

#### 5. Логирование и аудит
- ✅ Все действия администраторов логируются в таблицу `Log`
- ✅ Логирование добавления/удаления администраторов, изменения ролей
- ✅ Логирование варнов, банов, удаления сообщений
- ✅ Логирование регистрации пользователей
- ✅ Автоматическая очистка старых логов (каждые 24 часа):
  - Если логов больше 10000, оставляются только последние 10000
  - Иначе удаляются логи старше 30 дней

#### 6. Обработка канала
- ✅ Обработка новых постов в канале
- ✅ Проверка постов на запрещенный контент (blacklist)
- ✅ Автоматическое удаление постов с запрещенным контентом
- ✅ Автоматическое комментирование постов в связанной группе обсуждений:
  - При публикации нового поста бот автоматически оставляет первый комментарий
  - Комментарий генерируется через OpenAI GPT-4o-mini
  - Комментарий содержит реакцию на пост, а не пересказ содержания
  - Комментарий отправляется с задержкой 2 секунды после публикации поста
- ✅ Автоматический ответ на комментарии подписчиков:
  - При появлении комментариев других пользователей к постам бот отвечает следующим комментарием
  - Ответ генерируется через OpenAI GPT-4o-mini
  - Ответ содержит реакцию на комментарий пользователя
  - Ответ отправляется как reply к комментарию пользователя
- ✅ Модерация сообщений в группе обсуждений:
  - Проверка всех сообщений на баны
  - Проверка всех сообщений на blacklist
  - Удаление сообщений от забаненных пользователей
  - Удаление сообщений с запрещенным контентом

#### 7. Обработка медиа-контента
- ✅ Поддержка различных типов контента:
  - Текст сообщений
  - Фотографии с описанием через OpenAI Vision API
  - PDF документы (извлечение текста)
  - Документы: txt, docx, xlsx, pptx, odt (извлечение текста)
  - Голосовые сообщения (транскрипция через Whisper API)
  - Аудио файлы (транскрипция через Whisper API)
  - URL в тексте (извлечение контента с веб-страниц)
- ✅ Ограничения для работы на ограниченных ресурсах:
  - Максимальный размер файла: 10 MB
  - Максимальная длина извлеченного текста: 8000 символов
  - Таймауты для всех операций (10-60 секунд)
  - Обработка ошибок и fallback на базовый текст

## 🗄️ База данных

Проект использует **SQLite** (`app.db`) с асинхронным доступом через SQLAlchemy 2.0.

### Структура базы данных

#### Таблица `users`
Хранит информацию о пользователях бота:
- `id` (Integer, PK) — Telegram user_id
- `username` (String(64)) — @username
- `full_name` (String(128)) — полное имя
- `status` (Enum: ACTIVE, BANNED, RESTRICTED) — статус пользователя
- `warn_count` (Integer) — количество предупреждений
- `is_banned` (Boolean) — флаг бана (для обратной совместимости)
- `created_at` (DateTime) — дата регистрации

#### Таблица `admins`
Хранит информацию об администраторах бота:
- `id` (Integer, PK) — внутренний ID
- `user_id` (Integer, UNIQUE) — Telegram user_id
- `username` (String(64)) — @username
- `full_name` (String(128)) — полное имя администратора
- `role` (String(32)) — роль: `moderator`, `senior_admin`, `owner`
- `is_active` (Boolean) — активен ли администратор (soft delete)
- `added_by` (Integer) — user_id того, кто добавил администратора
- `created_at` (DateTime) — дата добавления
- `updated_at` (DateTime) — дата последнего обновления

#### Таблица `bans`
Хранит информацию о банах:
- `id` (Integer, PK) — внутренний ID
- `user_id` (Integer, FK → users.id) — забаненный пользователь
- `reason` (String(255)) — причина бана
- `until` (DateTime, NULL) — дата окончания бана (NULL = бессрочный)
- `created_at` (DateTime) — дата создания бана

#### Таблица `warns`
Хранит информацию о предупреждениях:
- `id` (Integer, PK) — внутренний ID
- `user_id` (Integer, FK → users.id) — пользователь, получивший предупреждение
- `reason` (String(255)) — причина предупреждения
- `created_at` (DateTime) — дата выдачи предупреждения

#### Таблица `blacklist`
Хранит запрещенные фразы:
- `id` (Integer, PK) — внутренний ID
- `phrase` (String(255), UNIQUE) — запрещенная фраза
- `created_at` (DateTime) — дата добавления
- `added_by` (Integer) — admin_id того, кто добавил фразу

#### Таблица `logs`
Хранит логи всех действий:
- `id` (Integer, PK) — внутренний ID
- `event_type` (String(32)) — тип события (admin_added, admin_removed, warn_added, user_banned, message_deleted_blacklist, и т.д.)
- `user_id` (Integer, FK → users.id, NULL) — пользователь, связанный с событием
- `message` (Text) — описание события
- `created_at` (DateTime) — дата события

#### Таблица `scheduled_posts` (⚠️ зарезервировано, не используется)
Модель существует в коде, но функциональность отложенных постов не реализована:
- `id` (Integer, PK)
- `text` (Text)
- `scheduled_at` (DateTime)
- `status` (Enum: PENDING, PUBLISHED, CANCELLED, FAILED)
- `created_at` (DateTime)
- `published_at` (DateTime, NULL)
- `channel_id` (Integer, NULL)

#### Таблица `ai_usage` (⚠️ зарезервировано, не используется)
Модель существует в коде, но функциональность логирования использования AI не реализована:
- `id` (Integer, PK)
- `request_type` (String(32))
- `tokens_used` (Integer, NULL)
- `created_at` (DateTime)
- `user_id` (Integer, FK → users.id, NULL)
- `success` (Boolean)
- `error_message` (Text, NULL)

### Инициализация базы данных

База данных автоматически создается при первом запуске бота. Все таблицы создаются автоматически при вызове `async_init_db()`.

При первом запуске администраторы из `.env` (переменная `ADMIN_IDS`) автоматически загружаются в таблицу `admins`:
- Первый администратор из списка получает роль `owner`
- Остальные администраторы получают роль `moderator`

## 🛠️ Дополнительные программы для отладки

### 1. `app/scripts/export_logs_to_excel.py`
Экспортирует последние 100 логов из базы данных в Excel файл (`.xlsx`) для анализа и отладки.

**Запуск:**
```bash
python -m app.scripts.export_logs_to_excel
```

**Возможности:**
- Экспорт последних N логов (по умолчанию 100)
- Форматированный Excel файл с заголовками и стилями
- Колонки: ID, Event Type, User ID, Message, Created At
- Автоматическое именование файла: `logs_export_YYYYMMDD_HHMMSS.xlsx`

### 2. `app/scripts/clear_warns_bans.py`
Очищает все варны и баны из базы данных, сбрасывает счетчики варнов у пользователей. Используется для тестирования.

**Запуск:**
```bash
python -m app.scripts.clear_warns_bans
# или
python app/scripts/clear_warns_bans.py
```

**Возможности:**
- Удаление всех записей из таблиц `warns` и `bans`
- Сброс счетчиков `warn_count` у всех пользователей
- Сброс статусов пользователей на `ACTIVE`
- Отображение статистики перед очисткой

### 3. `app/scripts/add_words_to_blacklist.py`
Добавляет конкретные слова в черный список без перезапуска полной инициализации.

**Запуск:**
```bash
python -m app.scripts.add_words_to_blacklist
# или
python app/scripts/add_words_to_blacklist.py
```

**Настройка:**
Отредактируйте список `WORDS_TO_ADD` в начале файла для добавления нужных слов.

### 4. `app/scripts/init_db.py`
Инициализирует базу данных и создает все таблицы (если они отсутствуют).

**Запуск:**
```bash
python -m app.scripts.init_db
```

### 5. `app/scripts/init_default_blacklist.py`
Загружает предварительный многоязычный черный список (русский, украинский, белорусский, казахский, английский, немецкий, французский).

**Запуск:**
```bash
python -m app.scripts.init_default_blacklist
```

### 6. `app/scripts/migrate_db.py`
Скрипт для миграции базы данных. Добавляет отсутствующие колонки и обновляет структуру.

**Примечание:** Скрипт дублирует функциональность `init_db.py` (оба вызывают `async_init_db()`). Можно использовать для ручной миграции, но не обязательно, так как миграция выполняется автоматически при старте бота.

**Запуск:**
```bash
python -m app.scripts.migrate_db
```

### 7. PowerShell скрипты (Windows)
- `check_bot_processes.ps1` — проверка запущенных процессов бота
- `check_bot_status.ps1` — проверка статуса бота
- `clear_warns_bans.ps1` — обертка для очистки варнов и банов

### 8. Batch скрипты (Windows)
- `clear_warns_bans.bat` — обертка для очистки варнов и банов

## 🏗️ Архитектура

Проект использует **Clean Architecture** с разделением на слои:

- **Presentation** — обработка Telegram API (aiogram 3.x)
  - Роутеры: `user_router`, `admin_router`, `channel_router`
  - Middleware: `admin_mw.py` существует, но не используется в `main.py` (проверки прав выполняются напрямую в роутерах)

- **Application** — бизнес-логика и сервисы
  - `user_service.py` — управление пользователями (регистрация, баны, варны, авторазбан)
  - `admin_service.py` — управление администраторами и проверка прав доступа
  - `moderation_service.py` — модерация (проверка blacklist с regex)
  - `stats_service.py` — статистика бота
  - `log_service.py` — работа с логами
  - `comment_service.py` — генерация комментариев через AI
  - `content_service.py` — обработка медиа-контента (фото, PDF, документы, аудио)

- **Infrastructure** — БД (SQLAlchemy), внешние сервисы
  - `models.py` — SQLAlchemy ORM модели
  - `repositories.py` — репозитории (Repository pattern)
  - `session.py` — настройка сессий и инициализация БД
  - `ai_clients.py` — клиенты для OpenAI и Google Gemini (через ProxyAPI)

- **Domain** — доменные модели (зарезервировано)

## ⚙️ Установка

1. Клонируйте репозиторий:
```bash
git clone <repository_url>
cd "I am an engineer"
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
venv\Scripts\activate  # Windows
# или
source venv/bin/activate  # Linux/Mac
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` в корне проекта:
```env
BOT_TOKEN=your_telegram_bot_token
ADMIN_IDS=123456789,987654321  # Начальные администраторы (разделены запятыми)
OWNER_ID=123456789  # Главный администратор (разработчик/заказчик)
DB_URL=sqlite+aiosqlite:///app.db
OPENAI_API_KEY=your_openai_api_key  # Для AI-функций (комментирование постов, ответы на комментарии)
```

5. Инициализируйте базу данных (опционально, создастся автоматически):
```bash
python -m app.scripts.init_db
```

6. (Опционально) Загрузите предварительный черный список:
```bash
python -m app.scripts.init_default_blacklist
```

## 🚀 Запуск

```bash
python -m app.main
```

Бот автоматически:
- Инициализирует базу данных (если нужно)
- Загрузит администраторов из `.env` в БД при первом запуске
- Инициализирует AI клиенты (если указан `OPENAI_API_KEY`)
- Запустит фоновую задачу для проверки истекших банов (каждый час)
- Запустит фоновую задачу для очистки старых логов (каждые 24 часа)
- Начнет обработку сообщений через polling

## 📚 Команды бота

### Пользовательские команды

- `/start` — регистрация в системе или обновление информации о пользователе (показывает Telegram ID)
- `/help` — справка по доступным командам
- `/myid` — показать свой Telegram user_id

### Админ-команды (модерация)

- `/ban {user_id} {reason}` — заблокировать пользователя (бессрочно или временно)
- `/warn {user_id} {reason}` — выдать предупреждение (3 варна = автоматический бан на 24 часа)
- `/blacklist add {phrase}` — добавить фразу в черный список
- `/blacklist remove {phrase}` — удалить фразу из черного списка
- `/blacklist list [page]` — показать черный список (10 фраз на страницу)
- `/stats` — статистика бота (пользователи, баны, варны, логи)

### Админ-команды (управление администраторами)

- `/addadmin {user_id} {role}` — добавить администратора
  - Роли: `moderator`, `senior_admin`, `owner`
  - Только `owner` может добавлять `senior_admin` и `owner`
  - `senior_admin` и `owner` могут добавлять `moderator`
- `/removeadmin {user_id}` — удалить администратора (soft delete)
  - `owner` может удалять всех
  - `senior_admin` может удалять `moderator`
  - `owner` не может быть удален
- `/admins` — список всех активных администраторов (с user_id для команд)
- `/myadmin` — информация о себе как администраторе
- `/setrole {user_id} {new_role}` — изменить роль администратора
  - Только `owner` может менять роли `senior_admin` и `owner`
  - `senior_admin` и `owner` могут менять роли `moderator`

## 🔐 Иерархия ролей администраторов

```
Owner (Владелец)
├─ Может добавлять/удалять всех администраторов
├─ Может менять любые роли
├─ Получает все уведомления
└─ Не может быть удален

Senior Admin (Старший администратор)
├─ Может добавлять/удалять moderator
├─ Может менять роли moderator
└─ Видит логи всех moderator

Moderator (Модератор)
├─ Может ban/warn пользователей
├─ Может управлять blacklist
├─ Видит статистику
└─ Не может добавлять админов
```

## 🔄 Автоматические процессы

1. **Автоматическая модерация сообщений:**
   - Проверка каждого сообщения на наличие запрещенных фраз
   - Автоматическое удаление сообщений с запрещенным контентом
   - Выдача предупреждений за нарушения

2. **Система предупреждений:**
   - При 3 предупреждениях пользователь автоматически получает бан на 24 часа
   - Уведомления пользователям о предупреждениях и банах в личные сообщения

3. **Автоматическое снятие банов:**
   - Фоновая задача проверяет истекшие баны каждый час
   - Автоматически снимает баны, у которых истек срок

4. **Обработка постов канала:**
   - При публикации нового поста в канале бот автоматически оставляет первый комментарий в связанной группе обсуждений
   - Комментарий генерируется через OpenAI GPT-4o-mini
   - Комментарий содержит реакцию на пост, а не пересказ содержания
   - Комментарий отправляется с задержкой 2 секунды после публикации поста
   - Реализовано в обработчике `new_channel_post_handler` (файл `app/presentation/routers/channel_router.py`)

5. **Ответы на комментарии пользователей:**
   - При появлении комментариев подписчиков канала к постам бот автоматически отвечает следующим комментарием
   - Ответ генерируется через OpenAI GPT-4o-mini
   - Ответ содержит реакцию на комментарий пользователя
   - Ответ отправляется как reply к комментарию пользователя
   - Реализовано в обработчике `discussion_message_handler` (файл `app/presentation/routers/channel_router.py`)

6. **Инициализация администраторов:**
   - При первом запуске администраторы из `.env` загружаются в БД
   - Первый администратор из списка получает роль `owner`

7. **Очистка старых логов:**
   - Фоновая задача очищает старые логи каждые 24 часа
   - Если логов больше 10000, оставляются только последние 10000
   - Иначе удаляются логи старше 30 дней

## 📊 Форматы уведомлений

### Уведомление при удалении сообщения (blacklist)

```
@username, ⚠️ Ваше сообщение удалено из-за использования запрещенных слов/выражений.
Предупреждений: 2/3
После трех предупреждений Вам запретят на 24 часа комментировать посты.
```

### Уведомление о предупреждении (команда /warn)

```
⚠️ Вам выдано предупреждение

Причина: {reason}
Текущее количество предупреждений: {warn_count}/3

При получении 3 предупреждений вы будете автоматически заблокированы на 24 часа.
```

### Уведомление о бане

```
❌ Вы были заблокированы

Причина: {reason}

Блокировка будет автоматически снята: {until_date} UTC
```

## 🔧 Технический стек

- **Python 3.10+**
- **aiogram 3.4.1** — фреймворк для Telegram Bot API
- **SQLAlchemy 2.0.23** — ORM для работы с БД
- **aiosqlite 0.19.0** — асинхронный драйвер для SQLite
- **pydantic 2.5.0** — валидация данных
- **pydantic-settings 2.1.0** — управление настройками
- **python-dotenv 1.0.0** — загрузка переменных окружения
- **openpyxl 3.1.2** — экспорт данных в Excel (для скрипта экспорта логов)
- **openai>=1.0.0** — клиент OpenAI API
- **aiohttp>=3.9.0** — асинхронные HTTP запросы
- **beautifulsoup4>=4.12.0** — парсинг HTML
- **pypdf>=3.17.0** — обработка PDF
- **python-docx>=1.1.0** — обработка Word документов
- **python-pptx>=0.6.23** — обработка PowerPoint презентаций

## 📁 Структура проекта

```
app/
├── main.py                    # Точка входа, инициализация БД, запуск polling
├── config/
│   └── settings.py           # Настройки приложения (pydantic-settings)
├── presentation/
│   ├── routers/              # Роутеры aiogram
│   │   ├── user_router.py    # Публичные команды (/start, /help, /myid)
│   │   ├── admin_router.py   # Админ-команды (/ban, /warn, /blacklist, /stats, /addadmin, /removeadmin, /admins, /myadmin, /setrole)
│   │   └── channel_router.py # Обработка канала и автоматическая модерация
│   └── middlewares/          # Middleware
│       └── admin_mw.py       # ⚠️ Существует, но не используется в main.py
├── application/
│   └── services/             # Бизнес-логика
│       ├── user_service.py   # Управление пользователями (регистрация, баны, варны, авторазбан)
│       ├── admin_service.py  # Управление администраторами и проверка прав
│       ├── moderation_service.py  # Модерация (проверка blacklist с regex)
│       ├── stats_service.py  # Статистика бота
│       ├── log_service.py    # Работа с логами
│       ├── comment_service.py # Генерация комментариев через AI
│       ├── content_service.py # Обработка медиа-контента
│       └── __init__.py       # Глобальные объекты для сервисов
├── infrastructure/
│   ├── ai_clients.py         # Клиенты для OpenAI и Gemini
│   └── db/
│       ├── models.py         # SQLAlchemy ORM модели
│       ├── session.py        # Настройка сессий и миграции БД
│       └── repositories.py   # Репозитории (Repository pattern)
└── scripts/
    ├── init_db.py            # Инициализация БД и создание таблиц
    ├── init_default_blacklist.py  # Предварительный многоязычный blacklist
    ├── add_words_to_blacklist.py  # Добавление слов в blacklist
    ├── clear_warns_bans.py   # Очистка варнов и банов из БД
    ├── export_logs_to_excel.py   # Экспорт логов в Excel для отладки
    └── migrate_db.py         # ⚠️ Дублирует функциональность init_db.py
```

## 🐛 Отладка

### Просмотр логов

1. **Через бота:**
   - Используйте команду `/stats` для просмотра общей статистики

2. **Через скрипт экспорта:**
   ```bash
   python -m app.scripts.export_logs_to_excel
   ```
   Создаст файл `logs_export_YYYYMMDD_HHMMSS.xlsx` с последними 100 логами

3. **Через базу данных:**
   - Используйте SQLite клиент для просмотра таблицы `logs`
   - Файл базы данных: `app.db`

### Очистка тестовых данных

Для очистки всех варнов и банов (при тестировании):
```bash
python -m app.scripts.clear_warns_bans
```

## ⚠️ Важные замечания

1. **Первый запуск:**
   - Убедитесь, что в `.env` указан `ADMIN_IDS` с первым администратором (получит роль `owner`)
   - При первом запуске администраторы из `.env` будут загружены в БД
   - Если база данных уже существует, администраторы не будут перезаписаны

2. **Права доступа:**
   - Все проверки прав администраторов происходят через базу данных
   - Статический список `ADMIN_IDS` из `.env` используется только для первичной инициализации

3. **Фоновые задачи:**
   - Проверка истекших банов происходит каждый час
   - Очистка старых логов происходит каждые 24 часа
   - При остановке бота (Ctrl+C) фоновые задачи корректно завершаются

4. **База данных:**
   - Используется SQLite для простоты развертывания
   - Легко переключиться на PostgreSQL, изменив `DB_URL` в `.env`
   - Автоматическая миграция: добавление отсутствующих колонок (status, warn_count) в таблицу users

5. **AI-функции:**
   - Бот использует OpenAI API через ProxyAPI для генерации комментариев и ответов
   - При недоступности OpenAI API AI-функции не работают, но бот продолжает работать в режиме модерации
   - Для работы AI-функций требуется переменная окружения `OPENAI_API_KEY` в `.env`

## 📝 Лицензия

MIT
