# Telegram Bot для канала "Безопасность всегда"

Telegram-бот для автоматической модерации канала и генерации AI-комментариев к постам. Бот обеспечивает безопасность контента, автоматически комментирует посты и отвечает на комментарии пользователей.

## 📋 Описание

Бот разработан для канала **"Безопасность всегда"** с девизом: *"Безопасность — это не случайность, а система."*

### Основные возможности:

- ✅ **Автоматическая модерация контента**
  - Проверка сообщений на запрещенные слова (blacklist)
  - **103 паттерна запрещенных слов** (русские и английские)
  - Умная проверка с учетом замен букв и обхода фильтров
  - Автоматические предупреждения (warns) за нарушения
  - Автоматический бан на 24 часа при 3+ предупреждениях
  - Проверка медиа-контента (фото, PDF, документы, аудио)

- 🤖 **AI-комментирование постов**
  - Автоматическая генерация первого комментария к каждому посту
  - Умные ответы на комментарии пользователей
  - Обработка медиа-контента через OpenAI (описание фото, транскрипция аудио, извлечение текста из PDF)

- 👮 **Система администраторов**
  - Иерархия ролей: `moderator`, `senior_admin`, `owner`
  - **Интерактивная админ-панель** с кнопками (`/admin`)
  - Управление администраторами через команды и панель
  - Статистика и логирование действий
  - Учет прав доступа для разных ролей

- 📊 **Логирование и статистика**
  - Все действия логируются в базу данных
  - Экспорт логов в Excel
  - Просмотр статистики через команду `/stats`

## 🚀 Быстрый старт

### Требования

- Python 3.11+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- OpenAI API Key (для AI-функций)

### Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository_url>
   cd Always_safe
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте конфигурацию:**
   ```bash
   # Скопируйте пример конфигурации
   cp env.example .env
   
   # Отредактируйте .env файл
   # Укажите ваш BOT_TOKEN, ADMIN_IDS, OPENAI_API_KEY
   ```

4. **Запустите бота:**
   ```bash
   python -m app.main
   ```

## ⚙️ Конфигурация

Создайте файл `.env` в корне проекта:

```env
# Telegram Bot Configuration
BOT_TOKEN=your_telegram_bot_token_here
ADMIN_IDS=123456789,987654321
OWNER_ID=123456789

# Database Configuration
DB_URL=sqlite+aiosqlite:///app.db

# OpenAI API Configuration (для AI-функций)
OPENAI_API_KEY=your_openai_api_key_here

# Google Gemini API Configuration (опционально)
GEMINI_API_KEY=your_gemini_api_key_here
```

### Параметры:

- `BOT_TOKEN` - токен бота от BotFather (обязательно)
- `ADMIN_IDS` - список ID администраторов через запятую (для первичной инициализации)
- `OWNER_ID` - ID главного администратора (owner)
- `DB_URL` - URL базы данных (по умолчанию SQLite)
- `OPENAI_API_KEY` - ключ OpenAI для AI-функций (обязательно для комментирования)
- `GEMINI_API_KEY` - ключ Gemini (опционально, не используется)

## 📁 Структура проекта

```
Always_safe/
├── app/
│   ├── main.py                 # Точка входа приложения
│   ├── config/
│   │   └── settings.py         # Настройки приложения
│   ├── infrastructure/
│   │   ├── db/                 # База данных (SQLAlchemy)
│   │   │   ├── models.py       # ORM модели
│   │   │   ├── repositories.py # Репозитории для работы с БД
│   │   │   └── session.py       # Настройка сессий БД
│   │   └── ai_clients.py       # Клиенты AI (OpenAI, Gemini)
│   ├── application/
│   │   └── services/           # Бизнес-логика
│   │       ├── admin_service.py
│   │       ├── comment_service.py    # Генерация комментариев через AI
│   │       ├── content_service.py    # Обработка медиа-контента
│   │       ├── moderation_service.py # Модерация (blacklist)
│   │       ├── user_service.py
│   │       └── stats_service.py
│   ├── presentation/
│   │   └── routers/            # Обработчики команд и сообщений
│   │       ├── admin_router.py # Команды администраторов
│   │       ├── user_router.py  # Пользовательские команды
│   │       └── channel_router.py # Обработка постов и комментариев
│   └── scripts/                # Утилиты
│       ├── view_logs.py        # Просмотр логов
│       ├── export_logs_to_excel.py
│       ├── clear_all_data.py
│       └── ...
├── app.db                      # База данных SQLite
├── .env                        # Конфигурация (не коммитится)
├── requirements.txt            # Зависимости Python
├── README.md                   # Этот файл
├── ADMIN_PANEL_INSTRUCTIONS.md # Инструкция по админ-панели
├── ADMIN_DATA_STORAGE.md       # Хранение данных администраторов
├── MODERATION_SYSTEM_DETAILS.md # Описание системы модерации
└── ...                         # Другая документация
```

## 🤖 Команды бота

### Пользовательские команды:

- `/start` - Регистрация пользователя
- `/help` - Справка по командам
- `/myid` - Показать свой Telegram ID

### Команды администраторов:

#### 🎛️ Интерактивная админ-панель (рекомендуется):
- `/admin` - Открыть админ-панель с кнопками
  - Управление пользователями
  - Управление blacklist
  - Статистика
  - Управление администраторами (только для senior_admin и owner)
  - Просмотр логов
  - Информация о себе

#### Текстовые команды:

##### Управление пользователями:
- `/ban {user_id} {reason}` - Заблокировать пользователя
- `/warn {user_id} {reason}` - Выдать предупреждение
- `/stats` - Показать статистику бота

##### Управление черным списком:
- `/blacklist add {phrase}` - Добавить фразу в черный список
- `/blacklist remove {phrase}` - Удалить фразу из черного списка
- `/blacklist list [page]` - Показать черный список (постранично)

##### Управление администраторами:
- `/addadmin {user_id} {role}` - Добавить администратора
  - Роли: `moderator`, `senior_admin`, `owner`
- `/removeadmin {user_id}` - Удалить администратора
- `/setrole {user_id} {new_role}` - Изменить роль администратора
- `/admins` - Показать список всех администраторов
- `/myadmin` - Показать информацию о себе как администраторе

**💡 Совет:** Используйте `/admin` для удобного управления через интерактивные кнопки!

## 🛠️ Скрипты и утилиты

### Просмотр логов:

```bash
# Последние 50 логов
python -m app.scripts.view_logs

# Последние 100 логов
python -m app.scripts.view_logs --limit 100

# Фильтр по типу события
python -m app.scripts.view_logs --type user_registered

# Фильтр по ID пользователя
python -m app.scripts.view_logs --user 123456789

# Сводка по логам
python -m app.scripts.view_logs --summary
```

### Экспорт логов в Excel:

```bash
python -m app.scripts.export_logs_to_excel
```

Файл сохранится в `./data/logs_export_YYYYMMDD_HHMMSS.xlsx`

### Очистка данных:

```bash
# Очистить все данные из БД (сохраняет структуру)
python -m app.scripts.clear_all_data

# Очистить только варны и баны
python -m app.scripts.clear_warns_bans
```

### Инициализация:

```bash
# Инициализация базы данных
python -m app.scripts.init_db

# Инициализация черного списка по умолчанию
python -m app.scripts.init_default_blacklist
```

### Проверка бота:

```bash
# Проверка вебхука и статуса бота
python check_and_fix_bot.py
```

## 🐳 Docker

Подробные инструкции по Docker см. в [DOCKER.md](DOCKER.md)

### Быстрый старт с Docker:

```bash
# Сборка и запуск
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

## 🔧 Особенности

### Автоматическая модерация:

- **Blacklist** - проверка на запрещенные слова с использованием регулярных выражений
- **103 паттерна запрещенных слов** - расширенная база русских и английских матерных слов
- **Умная проверка** - учет замен букв (а→a, о→0), регистра, множественных повторов
- **Защита от ложных срабатываний** - нормальные слова (тебя, небеса, ребенок) не блокируются
- **Автоматические варны** - за каждое нарушение blacklist выдается предупреждение
- **Автоматический бан** - при 3+ предупреждениях пользователь автоматически банится на 24 часа
- **Проверка медиа** - проверка фото (через AI описание), PDF, документов, аудио (транскрипция)

📖 Подробнее о системе модерации: [MODERATION_SYSTEM_DETAILS.md](MODERATION_SYSTEM_DETAILS.md)

### AI-комментирование:

- **Первый комментарий** - автоматически генерируется к каждому посту в канале
- **Ответы на комментарии** - бот отвечает на комментарии пользователей
- **Обработка медиа** - извлечение текста из PDF, описание фото, транскрипция аудио

### Система администраторов:

- **Интерактивная админ-панель** - удобное управление через кнопки (`/admin`)
- **Иерархия ролей:**
  - `moderator` - базовые права (баны, варны, blacklist)
  - `senior_admin` - может управлять модераторами
  - `owner` - полные права, может управлять всеми администраторами
- **Учет прав доступа** - панель показывает только доступные функции для каждой роли
- **Хранение данных** - все данные администраторов в таблице `admins` базы данных

📖 Подробнее:
- [ADMIN_PANEL_INSTRUCTIONS.md](ADMIN_PANEL_INSTRUCTIONS.md) - Инструкция по использованию админ-панели
- [ADMIN_DATA_STORAGE.md](ADMIN_DATA_STORAGE.md) - Где хранятся данные администраторов

### Фоновые задачи:

- Автоматическое снятие истекших банов (каждый час)
- Автоматическая очистка старых логов (каждые 24 часа)

## 📊 База данных

Бот использует SQLite базу данных (`app.db`) со следующими таблицами:

- `users` - пользователи бота
- `admins` - администраторы с ролями
- `bans` - баны пользователей (с датой окончания)
- `warns` - предупреждения пользователей
- `blacklist` - черный список запрещенных фраз
- `logs` - логи всех действий
- `scheduled_posts` - отложенные посты
- `ai_usage` - статистика использования AI

## 🔐 Безопасность

- Токены и ключи хранятся в `.env` файле (не коммитится в git)
- Проверка прав администраторов перед выполнением команд
- Логирование всех действий администраторов
- Автоматическая очистка старых логов для экономии места

## 📝 Логирование

Все действия бота логируются в базу данных:

- Регистрация пользователей
- Выдача варнов и банов
- Изменения в blacklist
- Действия администраторов
- Использование AI

Логи можно просматривать через скрипты или экспортировать в Excel.

## 🐛 Решение проблем

### Бот не запускается:

1. Проверьте, что все зависимости установлены: `pip install -r requirements.txt`
2. Проверьте файл `.env` - все обязательные параметры должны быть заполнены
3. Убедитесь, что `BOT_TOKEN` правильный

### Ошибка "TelegramConflictError":

Это означает, что уже запущен другой экземпляр бота. Решения:
1. Подождите 1-2 минуты после остановки предыдущего экземпляра
2. Проверьте, что бот не запущен в других терминалах
3. Используйте скрипт: `python check_and_fix_bot.py`

### AI не работает:

1. Проверьте, что `OPENAI_API_KEY` указан в `.env`
2. Проверьте баланс на счету OpenAI
3. Бот будет работать без AI, но комментирование будет отключено

## 📚 Дополнительная документация

### Основная документация:
- [DOCKER.md](DOCKER.md) - Инструкции по Docker
- [QUICKSTART_DOCKER.md](QUICKSTART_DOCKER.md) - Быстрый старт с Docker
- [GITHUB_PUSH_INSTRUCTIONS.md](GITHUB_PUSH_INSTRUCTIONS.md) - Инструкции по работе с GitHub

### Документация по функциям:
- [ADMIN_PANEL_INSTRUCTIONS.md](ADMIN_PANEL_INSTRUCTIONS.md) - Подробная инструкция по использованию админ-панели
- [ADMIN_DATA_STORAGE.md](ADMIN_DATA_STORAGE.md) - Где и как хранятся данные администраторов
- [MODERATION_SYSTEM_DETAILS.md](MODERATION_SYSTEM_DETAILS.md) - Подробное описание системы модерации

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект разработан для канала "Безопасность всегда".

## 👤 Автор

Разработано для автоматизации модерации и комментирования в Telegram-канале.

---

**Важно:** Не коммитьте файл `.env` в git! Он уже добавлен в `.gitignore`.
