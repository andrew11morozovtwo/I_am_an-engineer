# Информация, хранящаяся в app.db

База данных `app.db` (SQLite) содержит всю операционную информацию о работе бота. Ниже описано, какие данные хранятся в каждой таблице.

---

## 📊 Структура базы данных

### 1. Таблица `users` (Пользователи)

**Что хранится:**
- `id` - Telegram user_id (уникальный идентификатор пользователя)
- `username` - @username пользователя (если указан)
- `full_name` - Полное имя пользователя из Telegram
- `status` - Статус пользователя: `active`, `banned`, `restricted`
- `warn_count` - Количество предупреждений (варнов)
- `is_banned` - Флаг бана (для обратной совместимости)
- `created_at` - Дата регистрации пользователя в системе

**Когда создается:** Автоматически при первом взаимодействии пользователя с ботом (комментарий, команда и т.д.)

**Пример данных:**
```
id: 123456789
username: @username
full_name: Иван Иванов
status: active
warn_count: 2
created_at: 2026-01-21 10:30:00
```

---

### 2. Таблица `admins` (Администраторы)

**Что хранится:**
- `id` - Внутренний ID записи
- `user_id` - Telegram user_id администратора
- `username` - @username администратора
- `full_name` - Имя администратора
- `role` - Роль: `moderator`, `senior_admin`, `owner`
- `is_active` - Активен ли администратор
- `added_by` - user_id того, кто добавил администратора
- `created_at` - Дата добавления
- `updated_at` - Дата последнего обновления

**Когда создается:** При добавлении администратора через админ-панель (`/admin`)

**Пример данных:**
```
user_id: 123456789
username: @admin
full_name: Администратор
role: owner
is_active: true
added_by: null
```

---

### 3. Таблица `bans` (Баны)

**Что хранится:**
- `id` - Внутренний ID записи
- `user_id` - Telegram user_id забаненного пользователя
- `reason` - Причина бана (текст)
- `until` - Дата окончания бана (если временный) или NULL (если постоянный)
- `created_at` - Дата создания бана

**Когда создается:** 
- Автоматически при 3+ варнах
- Вручную администратором через админ-панель

**Пример данных:**
```
user_id: 123456789
reason: Автоматический бан за 3+ варнов (нарушение blacklist)
until: 2026-01-22 15:30:00  (временный бан на 24 часа)
created_at: 2026-01-21 15:30:00
```

---

### 4. Таблица `warns` (Предупреждения)

**Что хранится:**
- `id` - Внутренний ID записи
- `user_id` - Telegram user_id пользователя
- `reason` - Причина предупреждения
- `created_at` - Дата выдачи предупреждения

**Когда создается:**
- Автоматически при нарушении blacklist
- Вручную администратором через админ-панель

**Пример данных:**
```
user_id: 123456789
reason: Использование запрещенных слов/выражений (blacklist)
created_at: 2026-01-21 14:20:00
```

**Важно:** При 3+ варнах пользователь автоматически получает бан на 24 часа.

---

### 5. Таблица `blacklist` (Черный список)

**Что хранится:**
- `id` - Внутренний ID записи
- `phrase` - Запрещенная фраза/слово (уникальное)
- `created_at` - Дата добавления
- `added_by` - user_id администратора, который добавил (может быть NULL)

**Когда создается:** 
- При добавлении слова/фразы через админ-панель
- При инициализации базы данных (загружаются стандартные запрещенные слова)

**Пример данных:**
```
phrase: запрещенное_слово
added_by: 123456789
created_at: 2026-01-21 12:00:00
```

**Примечание:** В коде также есть 103 встроенных паттерна для проверки мата (они не хранятся в БД, а проверяются программно).

---

### 6. Таблица `logs` (Логи событий)

**Что хранится:**
- `id` - Внутренний ID записи
- `event_type` - Тип события (например: `message_deleted_blacklist`, `post_deleted`, `user_banned`)
- `user_id` - Telegram user_id пользователя (если применимо)
- `message` - Текст сообщения/описание события
- `created_at` - Дата и время события

**Когда создается:** При различных событиях в работе бота:
- Удаление сообщений
- Выдача варнов
- Баны пользователей
- Удаление постов
- И другие события

**Пример данных:**
```
event_type: message_deleted_blacklist
user_id: 123456789
message: Сообщение 12345 удалено из-за blacklist, выдан варн (2/3)
created_at: 2026-01-21 15:30:00
```

**Очистка:** Логи старше 30 дней автоматически удаляются фоновой задачей.

---

### 7. Таблица `scheduled_posts` (Отложенные посты)

**Что хранится:**
- `id` - Внутренний ID записи
- `text` - Текст поста для публикации
- `scheduled_at` - Дата/время запланированной публикации
- `status` - Статус: `pending`, `published`, `cancelled`, `failed`
- `created_at` - Дата создания записи
- `published_at` - Дата фактической публикации (если опубликован)
- `channel_id` - ID канала для публикации

**Когда создается:** При создании отложенного поста через админ-панель

**Пример данных:**
```
text: Важное напоминание о безопасности!
scheduled_at: 2026-01-22 10:00:00
status: pending
channel_id: -1001234567890
```

---

### 8. Таблица `ai_usage` (Использование ИИ)

**Что хранится:**
- `id` - Внутренний ID записи
- `request_type` - Тип запроса: `comment_generation`, `reply_generation`, `image_description`, `audio_transcription`
- `tokens_used` - Количество использованных токенов OpenAI
- `created_at` - Дата и время запроса
- `user_id` - Telegram user_id (если применимо)
- `success` - Успешность запроса (true/false)
- `error_message` - Сообщение об ошибке (если была)

**Когда создается:** При каждом обращении к OpenAI API

**Пример данных:**
```
request_type: comment_generation
tokens_used: 150
success: true
created_at: 2026-01-21 16:00:00
```

**Назначение:** Статистика использования ИИ для мониторинга расходов и анализа работы.

---

### 9. Таблица `post_comments` (История комментариев)

**Что хранится:**
- `id` - Внутренний ID записи
- `post_message_id` - ID поста в канале (для связи комментариев с постом)
- `comment_message_id` - ID комментария в группе обсуждений (уникальный)
- `user_id` - Telegram user_id пользователя (NULL если комментарий от бота)
- `is_bot_comment` - Флаг: комментарий от бота (true) или пользователя (false)
- `content` - Полный обработанный контент комментария:
  - Текст сообщения/подпись
  - Описание изображений (через OpenAI Vision)
  - Текст из PDF документов
  - Текст из документов (txt, docx, xlsx, pptx, odt)
  - Транскрипция аудио (через OpenAI Whisper)
- `content_type` - Тип контента: `text`, `photo`, `document`, `pdf`, `voice`, `audio`, `video`, `poll`
- `created_at` - Дата и время создания комментария

**Когда создается:**
- При генерации комментария ботом к посту
- При получении комментария от пользователя

**Пример данных:**
```
post_message_id: 12345
comment_message_id: 67890
user_id: 123456789
is_bot_comment: false
content: Это важное напоминание! Спасибо за пост.
content_type: text
created_at: 2026-01-21 17:00:00
```

**Очистка:** Комментарии старше 30 дней автоматически удаляются фоновой задачей.

**Назначение:** Используется для формирования истории обсуждения поста перед отправкой в ИИ для генерации ответов.

---

## 📈 Статистика данных

### Типичный размер базы данных:

- **Небольшой канал** (до 100 пользователей, до 1000 комментариев):
  - Размер БД: ~1-5 MB
  
- **Средний канал** (100-1000 пользователей, 1000-10000 комментариев):
  - Размер БД: ~5-50 MB
  
- **Большой канал** (1000+ пользователей, 10000+ комментариев):
  - Размер БД: ~50-500 MB

### Рост данных:

- **Логи:** Автоматически очищаются через 30 дней
- **Комментарии:** Автоматически очищаются через 30 дней
- **Пользователи, баны, варны:** Хранятся постоянно (можно очистить вручную)
- **Blacklist:** Хранится постоянно (можно редактировать)

---

## 🔒 Безопасность данных

### Что НЕ хранится в базе данных:

- ❌ **Пароли** - их нет (используется Telegram авторизация)
- ❌ **API ключи** - хранятся в `.env` файле (не в БД)
- ❌ **Токены бота** - хранятся в `.env` файле
- ❌ **Личные сообщения** - бот работает только в канале и группе обсуждений

### Что хранится (может быть чувствительным):

- ⚠️ **Telegram user_id** - публичный идентификатор (не секретный)
- ⚠️ **Username** - публичная информация из Telegram
- ⚠️ **Полное имя** - публичная информация из Telegram
- ⚠️ **Содержимое комментариев** - текст, который пользователи публикуют в группе обсуждений

**Важно:** Все данные, хранящиеся в БД, являются публичными (комментарии в группе обсуждений видны всем участникам).

---

## 🗑️ Очистка данных

### Автоматическая очистка:

1. **Логи старше 30 дней** - удаляются фоновой задачей каждые 24 часа
2. **Комментарии старше 30 дней** - удаляются фоновой задачей каждые 24 часа
3. **Истекшие баны** - проверяются каждый час, автоматически снимаются

### Ручная очистка:

Используйте скрипты из `app/scripts/`:
- `clear_all_data.py` - полная очистка всех данных
- `clear_warns_bans.py` - очистка варнов и банов
- `view_logs.py` - просмотр и экспорт логов

---

## 📝 Резюме

База данных `app.db` содержит:

1. ✅ **Пользовательские данные** - ID, username, имя, статус, варны
2. ✅ **Администраторы** - список админов с ролями
3. ✅ **Модерация** - баны, варны, blacklist
4. ✅ **Логи** - история событий (автоочистка через 30 дней)
5. ✅ **История комментариев** - для контекста ответов ИИ (автоочистка через 30 дней)
6. ✅ **Отложенные посты** - запланированные публикации
7. ✅ **Статистика ИИ** - использование токенов OpenAI

**Все данные являются операционными** и необходимы для работы бота. Чувствительные данные (API ключи, токены) хранятся в `.env` файле, который игнорируется Git.
