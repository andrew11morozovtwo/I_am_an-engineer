# Подготовка к отправке в GitHub

## ✅ Чек-лист перед коммитом

### 1. Проверка конфиденциальных данных

Убедитесь, что следующие файлы **НЕ** будут закоммичены:
- ✅ `.env` - файл с токенами (уже в .gitignore)
- ✅ `app.db` - база данных (уже в .gitignore)
- ✅ `data/` - директория с данными (уже в .gitignore)
- ✅ `__pycache__/` - кэш Python (уже в .gitignore)
- ✅ `*.log` - файлы логов (уже в .gitignore)

### 2. Проверка файлов на наличие токенов

**ВАЖНО:** Убедитесь, что в коде нет хардкоженных токенов или ключей!

Проверьте:
```bash
# Поиск возможных токенов в коде
grep -r "BOT_TOKEN\|OPENAI_API_KEY\|GEMINI_API_KEY" --include="*.py" | grep -v "env="
```

Если найдены хардкоженные токены - удалите их! Все токены должны быть в `.env` файле.

### 3. Файлы, которые должны быть в репозитории

✅ **Обязательно включить:**
- `README.md` - документация проекта
- `requirements.txt` - зависимости Python
- `env.example` - пример конфигурации (без реальных токенов!)
- `Dockerfile` - для Docker
- `docker-compose.yml` - конфигурация Docker
- `DOCKER.md` - инструкции по Docker
- `QUICKSTART_DOCKER.md` - быстрый старт
- Все файлы в `app/` (кроме `__pycache__/`)
- `.gitignore` - правила игнорирования файлов

❌ **НЕ включать:**
- `.env` - файл с реальными токенами
- `app.db` - база данных
- `data/` - директория с данными
- `__pycache__/` - кэш Python
- `*.log` - файлы логов
- `logs_export_*.xlsx` - экспортированные логи
- `check_bot_id.py` - временный скрипт
- `check_and_fix_bot.py` - временный скрипт

## 📋 Команды для подготовки

### 1. Проверка статуса Git

```bash
# Проверить, какие файлы будут добавлены
git status

# Проверить, что .env не будет закоммичен
git status | grep .env
# Должно быть пусто или показать "ignored"
```

### 2. Добавление файлов

```bash
# Добавить все файлы (кроме игнорируемых)
git add .

# Или добавить конкретные файлы
git add README.md
git add requirements.txt
git add app/
git add Dockerfile
git add docker-compose.yml
git add .gitignore
git add env.example
```

### 3. Проверка перед коммитом

```bash
# Посмотреть, что будет закоммичено
git status

# Проверить изменения
git diff --cached
```

### 4. Создание коммита

```bash
# Создать коммит
git commit -m "Initial commit: Telegram bot for safety channel

- Automatic moderation with blacklist
- AI-powered post commenting
- Admin management system
- Logging and statistics
- Docker support"
```

### 5. Настройка удаленного репозитория

```bash
# Добавить удаленный репозиторий (замените на ваш URL)
git remote add origin https://github.com/yourusername/Always_safe.git

# Или если репозиторий уже существует
git remote set-url origin https://github.com/yourusername/Always_safe.git

# Проверить удаленный репозиторий
git remote -v
```

### 6. Отправка в GitHub

```bash
# Отправить в GitHub
git push -u origin main

# Или если ветка называется master
git push -u origin master
```

## 🔒 Безопасность

### Если случайно закоммитили .env:

```bash
# Удалить файл из истории Git (ОСТОРОЖНО!)
git rm --cached .env
git commit -m "Remove .env from repository"

# Если уже отправили в GitHub:
# 1. Немедленно смените все токены в .env
# 2. Удалите файл из истории (см. инструкции ниже)
# 3. Отправьте исправление
```

### Смена токенов после утечки:

1. **Telegram Bot Token:**
   - Откройте [@BotFather](https://t.me/BotFather)
   - `/mybots` → выберите бота → `API Token` → `Revoke current token`
   - Получите новый токен

2. **OpenAI API Key:**
   - Зайдите в [OpenAI Dashboard](https://platform.openai.com/api-keys)
   - Удалите скомпрометированный ключ
   - Создайте новый ключ

## 📝 Рекомендуемая структура первого коммита

```
Initial commit: Telegram bot for safety channel

Features:
- Automatic content moderation with blacklist
- AI-powered post commenting via OpenAI
- Admin management system with role hierarchy
- Comprehensive logging and statistics
- Docker support for easy deployment

Technical:
- Python 3.11+ with aiogram 3.4.1
- SQLite database with SQLAlchemy ORM
- Clean architecture (presentation/application/infrastructure)
- Async/await throughout
```

## ⚠️ Важные напоминания

1. **НИКОГДА** не коммитьте файл `.env` с реальными токенами
2. **ВСЕГДА** используйте `env.example` как шаблон
3. **ПРОВЕРЯЙТЕ** `git status` перед каждым коммитом
4. **ИСПОЛЬЗУЙТЕ** `.gitignore` для исключения конфиденциальных файлов
5. **ОБНОВЛЯЙТЕ** README.md при изменении функционала

## 🚀 После отправки в GitHub

1. Проверьте, что все файлы загружены правильно
2. Убедитесь, что `.env` НЕ виден в репозитории
3. Обновите описание репозитория на GitHub
4. Добавьте теги (tags) для версий, если нужно
5. Настройте GitHub Actions для CI/CD (опционально)

---

**Готово к отправке!** 🎉
